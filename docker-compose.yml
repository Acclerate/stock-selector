services:
  stock-selector:
    build: .
    ports:
      - "5002:5001"
    env_file:
      - .env
    volumes:
      # 代码目录 bind mount — 改代码立即生效，无需 rebuild
      - ./app:/app/app
      - ./data:/app/data
      - ./selectors:/app/selectors
      - ./utils:/app/utils
    restart: unless-stopped
